 - GRPO
   - Create the reward function for the maze
   - Write the training loop:
      - Sample N number of paths, storing the output probs
         - For this I think we're going to need to write a batched inference? But with gradients
      - Calculate the rewards over all of them
      - Calculate the importance sampling ratio using the current model + saved output probs
      - Calculate the loss
      - loss.backward()

 - Positional embedding should be 2d
    - What if you still had the normal embeddings, but you also did a separate embedding for row_idx, col_idx and then added those in for the positional ones
 - Build a nice visualizer for the maze
 - Make the loss calculations more efficient
