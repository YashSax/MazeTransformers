entity: "whysax-agnetic-labs"
project: "MazeSFT"
name: "Initial"

model:
  name: "smart_boi_short"
  vocab_size: 10
  output_vocab_size: 5
  d_model: 256
  n_layers: 10
  max_seq_len: 64
  n_heads: 4
  dropout: 0
  batch_size: 128
  num_epochs: 50
  learning_rate: 0.00005
  device: mps
  output_dir: models/

dataset:
  size: 8
  num_train: 8192
  num_test: 1024
  output_dir: data
