model:
  vocab_size: 10
  output_vocab_size: 5
  d_model: 64
  n_layers: 3
  max_seq_len: 32
  n_heads: 1
  dropout: 0
  batch_size: 256
  num_epochs: 30
  learning_rate: 0.00001
  device: cpu

dataset:
  size: 2
  num_train: 2048
  num_test: 32
  output_dir: data
